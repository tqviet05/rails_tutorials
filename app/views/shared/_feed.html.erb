<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items, params: { controller: :static_pages, action: :home} %>
  <%# Khong hieu vi sao the tham so params: { controller: :static_pages, action: :} mucj 13.3.3 %>
  <% end %>
